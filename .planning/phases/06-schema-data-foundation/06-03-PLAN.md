---
phase: 06-schema-data-foundation
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/api/meal-plans/route.ts
  - src/components/week-planner.tsx
  - src/components/recipe-picker.tsx
autonomous: false
requirements: [DB-03]

must_haves:
  truths:
    - "User can add two different meals on the same day for the same person (e.g., Mittag + Abend)"
    - "Existing meal plan entries still display correctly (migrated as Abend)"
    - "Week planner shows only filled meal slots plus an Add button per day"
    - "New meal entries include a mealType selection (default: abend)"
  artifacts:
    - path: "src/app/api/meal-plans/route.ts"
      provides: "API supporting mealType in POST/PUT/GET, 3-field composite key upsert"
      exports: ["GET", "POST", "PUT", "DELETE"]
    - path: "src/components/week-planner.tsx"
      provides: "Dynamic slot rendering grouped by mealType"
    - path: "src/components/recipe-picker.tsx"
      provides: "MealType selector in recipe picker"
  key_links:
    - from: "src/components/week-planner.tsx"
      to: "/api/meal-plans"
      via: "fetch with mealType parameter"
      pattern: "mealType"
    - from: "src/app/api/meal-plans/route.ts"
      to: "prisma.mealPlan"
      via: "upsert with date_forUserId_mealType composite key"
      pattern: "date_forUserId_mealType"
---

<objective>
Update the week planner UI and meal-plans API to support the new MealType enum, enabling multiple meals per day per user with a compact slot-based layout.

Purpose: The MealPlan schema now supports mealType but the API and UI still use the old 2-field composite key. This plan wires the new mealType through the full stack so users can plan Mittag and Abend (and optionally Fruhstuck/Snacks) separately.
Output: Working week planner with mealType-aware slots, API accepting mealType parameter.
</objective>

<execution_context>
@/Users/markuszabel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markuszabel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-schema-data-foundation/06-CONTEXT.md
@.planning/phases/06-schema-data-foundation/06-RESEARCH.md
@.planning/phases/06-schema-data-foundation/06-01-SUMMARY.md

@src/app/api/meal-plans/route.ts
@src/components/week-planner.tsx
@src/components/recipe-picker.tsx

<interfaces>
<!-- Current API contract (must be updated) -->

From src/app/api/meal-plans/route.ts:
- POST body: { date, recipeId, customMeal, forUserIds }
- PUT body: { id, recipeId, customMeal, date, forUserIds, overwrite }
- Upsert uses composite key: date_forUserId (2 fields)
- Must change to: date_forUserId_mealType (3 fields)

From src/components/week-planner.tsx:
- MealPlanEntry type: { id, date, recipeId, customMeal, recipe, forUser, assigner }
- Must add: mealType field
- groupEntries() groups by recipe.id or custom meal text
- Must change: group by mealType first, then by recipe within each mealType
- allAssigned check: entries.length >= familyMembers.length (hides Add button)
- Must change: Add button always visible (user can add meals for different mealTypes)

MealType enum values (from schema): fruehstueck, mittag, abend, snacks
MealType display labels: Fruhstuck, Mittag, Abend, Snacks
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update meal-plans API for mealType support</name>
  <files>src/app/api/meal-plans/route.ts</files>
  <action>
Update `src/app/api/meal-plans/route.ts` to support the new 3-field composite key:

1. **GET:** Add `mealType` to the `include` response (it is already a field on the model, just needs to be returned). Add `orderBy: [{ date: "asc" }, { mealType: "asc" }]` for consistent ordering.

2. **POST:** Accept `mealType` in request body (default to `"abend"` if not provided for backward compatibility). Change the upsert `where` from `date_forUserId` to `date_forUserId_mealType`:
   ```
   where: {
     date_forUserId_mealType: {
       date: new Date(date),
       forUserId,
       mealType: mealType || "abend",
     },
   },
   ```
   Include `mealType` in both `update` and `create` objects.

3. **PUT:** Accept `mealType` in request body. Update conflict detection to include mealType:
   ```
   where: {
     date: targetDate,
     forUserId: { in: targetUserIds },
     mealType: targetMealType,
     id: { not: id },
   }
   ```
   Update the upsert `where` to use the 3-field composite key. Include `mealType` in create/update.

4. **DELETE:** No changes needed (deletes by id).

5. **Response:** Ensure all responses include `mealType` field in the returned objects (add to `select` or ensure it's included by default).

Import the `MealType` enum from `@/generated/prisma/client` for type safety. Validate that the provided mealType value is valid.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/app/api/meal-plans/route.ts</automated>
  </verify>
  <done>
  - API accepts mealType parameter in POST and PUT
  - Defaults to "abend" when mealType not provided
  - Upsert uses 3-field composite key (date + forUserId + mealType)
  - All responses include mealType field
  - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Update week planner UI for mealType slots</name>
  <files>src/components/week-planner.tsx, src/components/recipe-picker.tsx</files>
  <action>
Update `src/components/week-planner.tsx`:

1. **MealPlanEntry type:** Add `mealType: "fruehstueck" | "mittag" | "abend" | "snacks"` field.

2. **Meal type labels:** Add a constant map for display labels:
   ```
   const MEAL_TYPE_LABELS: Record<string, string> = {
     fruehstueck: "Fruhstuck",
     mittag: "Mittag",
     abend: "Abend",
     snacks: "Snacks",
   };
   ```

3. **Group by mealType:** Change `groupEntries()` to first group entries by `mealType`, then within each mealType group by recipe/customMeal as before. The result should be an ordered list of mealType sections, each containing meal groups with their members.

4. **Render mealType sections:** For each day, render filled mealType sections with a small label (e.g., "Mittag" in muted text above the meal entry). Show entries grouped under their mealType. Only show mealType labels when there are entries for that type — do NOT show all 4 slots.

5. **Add button:** Always show the "+ Gericht wahlen" add button per day (remove the `allAssigned` check that hides it). When clicking Add, the user should be able to select which mealType to assign.

6. **PickerTarget:** Add `mealType` to the PickerTarget type. When clicking Add, default mealType to "abend". When editing, carry the existing entry's mealType.

7. **assignMeal:** Include `mealType` in both POST and PUT fetch calls.

Update `src/components/recipe-picker.tsx`:

8. **MealType selector:** Add a mealType dropdown/selector at the top of the RecipePicker (above the recipe search). Show 4 options: Fruhstuck, Mittag, Abend, Snacks. Default to the mealType from the picker target (or "abend"). When editing, pre-select the current mealType.

9. **Pass mealType to onSelect:** The RecipePicker's `onSelect` callback must include the selected mealType so that `assignMeal` in week-planner can pass it to the API.

Keep the UI compact per user decision: only filled slots visible + Add button. Do not show empty mealType slots.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/week-planner.tsx src/components/recipe-picker.tsx</automated>
  </verify>
  <done>
  - Week planner renders meals grouped by mealType with section labels
  - Only filled mealType slots are shown (compact layout)
  - Add button always visible per day
  - MealType selector appears in RecipePicker
  - Creating a meal for "Mittag" and another for "Abend" on the same day works without conflicts
  - TypeScript compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify week planner mealType UI</name>
  <action>
Human verifies the week planner UI with mealType support works correctly: multiple meals per day, compact slot layout, mealType selector in recipe picker.
  </action>
  <verify>User confirms visual and functional correctness</verify>
  <done>User approves the week planner mealType UI</done>
  <what-built>Week planner with mealType support: multiple meals per day, compact slot layout, mealType selector in recipe picker</what-built>
  <how-to-verify>
    1. Open the app in browser (http://localhost:3000 or production URL)
    2. Navigate to the week planner
    3. Verify existing meals show with "Abend" label (migrated data)
    4. Click "+ Gericht wahlen" on any day
    5. Verify a mealType selector appears (Fruhstuck, Mittag, Abend, Snacks)
    6. Select "Mittag" and assign a recipe
    7. Select "Abend" and assign a different recipe to the same day
    8. Verify both meals appear on the same day with their mealType labels
    9. Verify the layout is compact (only filled slots shown, not all 4)
    10. Edit an existing meal — verify mealType is preserved
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for all modified files
- API POST with `{ date, recipeId, forUserIds, mealType: "mittag" }` returns 201
- API POST with same date/user but `mealType: "abend"` also returns 201 (no conflict)
- Week planner renders both meals on the same day
- Existing meals display correctly as "Abend"
</verification>

<success_criteria>
- Multiple meals per day per user works end-to-end (API + UI)
- Week planner shows compact mealType-grouped layout
- No regression on existing meal plan functionality
- User confirms visual correctness via checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/06-schema-data-foundation/06-03-SUMMARY.md`
</output>
