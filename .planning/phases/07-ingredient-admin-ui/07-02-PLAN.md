---
phase: 07-ingredient-admin-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/app/(app)/zutaten/page.tsx
  - src/app/(app)/zutaten/neu/page.tsx
  - src/app/(app)/zutaten/[id]/bearbeiten/page.tsx
autonomous: false

requirements:
  - VIEW-05

must_haves:
  truths:
    - "User can navigate to /zutaten and see a searchable list of ingredients"
    - "User can search ingredients by German or English name with fuzzy matching"
    - "User can create a new custom ingredient via /zutaten/neu"
    - "User can edit any ingredient's name and nutrition values via /zutaten/[id]/bearbeiten"
    - "User can delete a custom ingredient with two-step confirmation"
    - "USDA ingredients cannot be deleted (no delete button shown)"
  artifacts:
    - path: "src/app/(app)/zutaten/page.tsx"
      provides: "Searchable ingredient list page"
      min_lines: 40
    - path: "src/app/(app)/zutaten/neu/page.tsx"
      provides: "New ingredient page with IngredientForm"
      min_lines: 10
    - path: "src/app/(app)/zutaten/[id]/bearbeiten/page.tsx"
      provides: "Edit ingredient page loading existing data"
      min_lines: 20
  key_links:
    - from: "src/app/(app)/zutaten/page.tsx"
      to: "/api/ingredients"
      via: "fetch in useEffect triggered by search state"
      pattern: "fetch.*api/ingredients"
    - from: "src/app/(app)/zutaten/neu/page.tsx"
      to: "src/components/ingredient-form.tsx"
      via: "IngredientForm component (no initial prop = create mode)"
      pattern: "IngredientForm"
    - from: "src/app/(app)/zutaten/[id]/bearbeiten/page.tsx"
      to: "src/components/ingredient-form.tsx"
      via: "IngredientForm with initial prop (edit mode)"
      pattern: "IngredientForm.*initial"
---

<objective>
Build the three ingredient page routes (list, create, edit) that wire up the API and components from Plan 01 into a complete ingredient management UI.

Purpose: Delivers the user-facing ingredient admin feature (VIEW-05) — search, add, edit, and delete ingredients.
Output: 3 page routes completing the full CRUD flow accessible via the bottom nav "Zutaten" link.
</objective>

<execution_context>
@/Users/markuszabel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markuszabel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ingredient-admin-ui/07-RESEARCH.md
@.planning/phases/07-ingredient-admin-ui/07-01-SUMMARY.md

@src/app/(app)/rezepte/page.tsx
@src/app/(app)/rezepte/[id]/page.tsx
@src/app/(app)/rezepte/neu/page.tsx
@src/components/ingredient-form.tsx
@src/components/delete-ingredient-button.tsx

<interfaces>
<!-- From Plan 01 outputs -->
From src/components/ingredient-form.tsx:
  export function IngredientForm({ initial }: { initial?: IngredientData })
  // IngredientData = { id, name, nameEn, kcalPer100g, proteinPer100g, fatPer100g, satFatPer100g, carbsPer100g, sugarPer100g, fiberPer100g }

From src/components/delete-ingredient-button.tsx:
  export function DeleteIngredientButton({ id, isCustom }: { id: string; isCustom: boolean })
  // Only renders button when isCustom=true

API contracts (from Plan 01):
  GET /api/ingredients?search=term  -> IngredientRow[] (max 50, fuzzy-ranked)
  GET /api/ingredients              -> IngredientRow[] (max 100, custom-first then alpha)
  POST /api/ingredients             -> Ingredient (201)
  PUT /api/ingredients/[id]         -> Ingredient (200)
  DELETE /api/ingredients/[id]      -> { success: true } (200) or 403 for USDA
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ingredient list page with search</name>
  <files>src/app/(app)/zutaten/page.tsx</files>
  <action>
Create a `"use client"` list page mirroring `src/app/(app)/rezepte/page.tsx` pattern.

Page structure:
- Title "Zutaten" at the top with a "Neue Zutat" link/button (styled as primary button, links to `/zutaten/neu`)
- Search input with placeholder "Zutat suchen..." — controlled via `useState`, triggers fetch on change
- Loading state while fetching (simple "Laden..." text or spinner matching recipe pattern)
- Results list showing each ingredient as a card/row with: name (bold), nameEn in parentheses if present, kcalPer100g + proteinPer100g + fatPer100g + carbsPer100g shown as compact nutrition summary (e.g. "245 kcal | 12g P | 8g F | 30g KH")
- Each row links to `/zutaten/${ingredient.id}/bearbeiten` (no separate detail page — edit directly)
- Show a subtle badge or indicator for custom ingredients (e.g., small "Eigene" tag)
- If no results found after search, show "Keine Zutaten gefunden"
- If search is empty and results loaded, show a note like "Zeigt die ersten 100 Zutaten. Suche eingeben fuer mehr Ergebnisse."

Data fetching:
- `useEffect` on `[search]` dependency — fetch `/api/ingredients?search=${encodeURIComponent(search)}` when search has content, or `/api/ingredients` when empty
- Debounce is optional but not required (the API already limits to 50 results and pg_trgm is fast)

Styling: Match recipe list page Tailwind classes. Use the same card/list item pattern. Ensure bottom padding for the nav bar (`pb-24` or similar).
  </action>
  <verify>
    <automated>cd /Users/markuszabel/Development/meal-planner && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Navigating to /zutaten shows a searchable list of ingredients loaded from the API. Search input triggers filtered results. Each item links to edit page.</done>
</task>

<task type="auto">
  <name>Task 2: Create new and edit ingredient pages</name>
  <files>src/app/(app)/zutaten/neu/page.tsx, src/app/(app)/zutaten/[id]/bearbeiten/page.tsx</files>
  <action>
**src/app/(app)/zutaten/neu/page.tsx — New ingredient page:**
Simple page that renders `IngredientForm` with no `initial` prop (create mode). Add a breadcrumb or back link to `/zutaten`. Use Tailwind classes matching the recipe "neu" page. This can be a client component or a thin server component wrapping the client form — follow whichever pattern the recipe "neu" page uses.

**src/app/(app)/zutaten/[id]/bearbeiten/page.tsx — Edit ingredient page:**
Server component (async) that loads the ingredient by ID using `prisma.ingredient.findUnique({ where: { id } })`. If not found, call `notFound()` from `next/navigation`. Pass loaded ingredient data as `initial` prop to `IngredientForm`. Also render `DeleteIngredientButton` below the form, passing `id` and `isCustom`.

Add a breadcrumb or back link to `/zutaten`. Include the ingredient name in the page title (e.g., "Milch bearbeiten").

Import prisma from `@/lib/db`. Use `const { id } = await params` for the dynamic route param (Next.js 16 async params). The page function signature: `export default async function BearbeitenPage({ params }: { params: Promise<{ id: string }> })`.

Ensure the edit page loads all Ingredient fields needed by the form: name, nameEn, kcalPer100g, proteinPer100g, fatPer100g, satFatPer100g, carbsPer100g, sugarPer100g, fiberPer100g, isCustom.
  </action>
  <verify>
    <automated>cd /Users/markuszabel/Development/meal-planner && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>/zutaten/neu renders the IngredientForm in create mode. /zutaten/[id]/bearbeiten loads ingredient data from DB and renders form in edit mode with delete button below. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete ingredient admin UI</name>
  <action>Human verifies the full ingredient CRUD flow works end-to-end on desktop and mobile.</action>
  <what-built>Complete ingredient admin UI: searchable list at /zutaten, create form at /zutaten/neu, edit form at /zutaten/[id]/bearbeiten, delete with two-step confirm, nav bar with 5th "Zutaten" item.</what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Navigate to the app — bottom nav should show 5 items including "Zutaten"
    3. Tap "Zutaten" — should see list of first 100 ingredients with nutrition summary
    4. Type "Milch" in search — should see fuzzy-matched results (Milch, Buttermilch, etc.)
    5. Type "Ei" (2 chars) — should return results including Eier, Eiweisspulver, etc.
    6. Tap "Neue Zutat" — should see create form with name + nutrition fields
    7. Fill in: Name="Test Zutat", Kalorien=100 — submit. Should redirect to /zutaten
    8. Search for "Test Zutat" — should appear with "Eigene" badge
    9. Tap "Test Zutat" to edit — form should be pre-filled with saved values
    10. Change Kalorien to 200, save — should update
    11. Edit "Test Zutat" again — "Loeschen" button should be visible (it's custom)
    12. Delete "Test Zutat" with two-step confirm — should redirect to /zutaten
    13. Edit any USDA ingredient (e.g., "Milch") — no delete button should appear
    14. Check on mobile viewport (375px) — 5 nav items should fit without label wrapping
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Full CRUD flow works: list, search, create, edit, delete
- USDA entries are editable but not deletable
- Custom entries show delete button, USDA entries do not
- Nav bar works with 5 items on mobile (375px)
- pg_trgm fuzzy search returns relevant results for German ingredient names
</verification>

<success_criteria>
- User can navigate to /zutaten from bottom nav and see searchable ingredient list
- User can create a new custom ingredient with name and nutrition values
- User can edit any ingredient's details
- User can delete only custom ingredients (USDA entries protected)
- Search returns fuzzy-matched results for queries >= 2 characters
- Mobile layout (375px) renders correctly with 5 nav items
</success_criteria>

<output>
After completion, create `.planning/phases/07-ingredient-admin-ui/07-02-SUMMARY.md`
</output>
